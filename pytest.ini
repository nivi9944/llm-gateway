[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
markers =
    slow: needs the real MiniLM model (downloads ~90 MB on first run)
filterwarnings =
    ignore::DeprecationWarning
